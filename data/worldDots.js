(() => {
  // Static high-quality world map - no procedural generation needed
  // This gives consistent, recognizable continents on every render
  window.XGlobeWorldDots = null; // Renderer will use continents.js
})();
